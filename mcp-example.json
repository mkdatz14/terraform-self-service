{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-github"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "GitHub MCP server for repository and code management"
    },
    "terraform": {
      "command": "npx",
      "args": [
        "-y",
        "@cloudflare/mcp-server-terraform"
      ],
      "env": {
        "TFE_TOKEN": "${TFE_TOKEN}",
        "TFE_ORGANIZATION": "${TFE_ORG}",
        "TFE_HOSTNAME": "app.terraform.io"
      },
      "description": "Terraform MCP server with HCP Terraform integration and enterprise tools"
    }
  },
  "metadata": {
    "name": "self-service-hcp-terraform-workflow",
    "version": "1.0.0",
    "description": "MCP server configuration for self-service HCP Terraform workflow with GitHub integration",
    "documentation": "See README.md and docs/ folder for complete setup instructions"
  }
}
